Versione 1.0 (senza strutture di controllo):

front-end che termina con la produzione dell'IR

Aggiungendo l'opzione -v  viene anche prodotto (su stdout) una rappresentazione dell'AST.

E' possibile anche creare un file oggetto (opzione -o <nomefile>).
